{
  "mcpServers": {
    "ai-lecture-workspace": {
      "type": "stdio",
      "command": "${PYTHON_COMMAND:-python}",
      "args": [
        "${CLAUDE_PROJECT_DIR:-.}/scripts/run_mcp.py"
      ],
      "timeout": 120000
    }
  }
}
