# Latest Agent Handoff

- **Task:** T030 — Implement the Phase-1 workflow job queue and approval states
- **Owner:** codex
- **Status:** REVIEW — implementation and complete local validation passed; independent or human review is required
- **Branch/PR:** `task/t030-workflow-queue-approvals`; draft PR #6 targets `main`: `https://github.com/Unique-Group-of-Institution/ai-lecture-system/pull/6`
- **Architecture:** Authentication-provider-independent, prevalidated `WorkflowActorContext` with explicit teacher, administrator and system-worker capabilities. Django currently authenticates teacher/administrator operations only; CRM/SSO sessions and tables remain deferred.
- **State machine:** Explicit source/content-ready, slide/narration draft and teacher approval, recording pending/ready, draft-video pending/ready, teacher revision/approval, final administrator approval and export-ready states. Current-version checks, allowlisted reasons and idempotency keys reject stale, duplicate, backward or skipped transitions.
- **Approval gates:** Current T022 slide revisions are fingerprinted at teacher approval. A later revision atomically invalidates downstream state and audits the rollback. Administrators cannot replace teacher approvals; teachers cannot grant final administrator approval; teacher video approval precedes final admin approval; export-ready requires final admin approval and a successful matching job.
- **Artifact/result binding:** Four strict completion schemas bind `job_id`, `job_type`, `workflow_id` and `workflow_version` to the exact typed generation/recording/video/export result. Transitions atomically revalidate the successful job payload/result, reject missing, malformed, cross-context or substituted references, and persist the reference taken from validated job evidence.
- **Trusted-worker boundary:** No trusted HTTP worker authentication exists in Phase 1. Administrators are never converted into workers; request JSON, headers and query parameters cannot supply worker identity, course scope or capabilities. HTTP claim/complete/fail and system-only transitions return controlled forbidden responses. Administrative inspect/submit/cancel and explicit admin approvals remain available. Future trusted local adapters may call the domain with a prevalidated `WorkflowActorContext` after separate authorization.
- **Queue:** Four exact bounded reference-only job types; no commands, arbitrary code, credentials or paths. Database transactions, PostgreSQL row locks/`SKIP LOCKED`, attempt limits, leases, expiry recovery, bounded retries, idempotent submission/completion, pending-only cancellation, privacy-safe failures and immutable events remain implemented. Job completion never advances state automatically.
- **Concurrency:** SQLite is sequential/single-worker only and is not claimed safe for multi-worker production. PostgreSQL plus a controlled worker deployment is mandatory before concurrency.
- **Models/APIs:** Migration `0006` adds `LectureWorkflow`, `WorkflowAuditEvent`, `WorkflowJob` and `WorkflowJobEvent`. Minimal scoped workflow/transition/audit and queue submit/inspect/cancel JSON APIs remain active; worker HTTP routes are reserved but disabled; Django admin paths are read-only.
- **Scope exclusions:** No recording UI, audio/video processing, voice clone, export generation, uploader action, CRM integration, paid dependency, Redis/Celery/cloud queue, stronger AI provider or real/private artifact access.
- **Verification:** Synthetic-only 20 focused T030 tests, 90 complete Django tests and 19 complete repository tests passed. Workspace, system, migration drift, clean disposable migration, compilation, dependency, task/dashboard, privacy/ignore and diff checks passed. No real or private artifact was accessed.
- **Publication:** The prior reviewed head was `a1bbe6e8cf2d643a569e704994070fb9a90f01fb`. Push only the two HIGH corrections and their tests/documentation through the active hook, then require fresh `workspace-ci` success on the exact new PR head. Keep PR #6 draft and T030 in REVIEW; do not approve, mark ready, merge or self-complete T030.
