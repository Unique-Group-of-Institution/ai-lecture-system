# Project-scoped Codex MCP configuration.
# Keep disabled until scripts/setup_windows.ps1 or scripts/setup_posix.sh succeeds.
[mcp_servers.ai_lecture_workspace]
command = "python"
args = ["scripts/run_mcp.py"]
cwd = "."
enabled = false
required = false
startup_timeout_sec = 20
tool_timeout_sec = 120
default_tools_approval_mode = "writes"

